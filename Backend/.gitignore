# --- DEPENDENCIAS ---
node_modules/
jspm_packages/

# --- COMPILACIÓN ---
# Ignoramos la carpeta de salida (JS convertido)
dist/
build/
out/
# Archivo que guarda el estado de la compilación incremental de TS
*.tsbuildinfo

# --- SEGURIDAD (CRÍTICO) ---
# Nunca subir credenciales de Supabase o Gmail
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
*.env

# --- LOGS Y DEPURACIÓN ---
npm-debug.log*
yarn-debug.log*
yarn-error.log*
logs/
*.log
# Reportes de cobertura de tests
coverage/

# --- ARCHIVOS DE SISTEMA ---
.DS_Store
Thumbs.db
*.swp
desktop.ini

# --- CONFIGURACIÓN DE EDITOR ---
# Ignoramos configuraciones personales, pero permitimos las recomendadas si el equipo las usa
.vscode/*
!.vscode/extensions.json
.idea/
*.suo
*.ntvs*
*.njsproj
*.sln

# --- ARCHIVOS DE LA APLICACIÓN ---
# Carpeta donde se guardan temporalmente las fotos de pacientes o PDFs
uploads/
temp/

# --- OTROS ---
.cache/